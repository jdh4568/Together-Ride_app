import 'package:flutter/material.dart';

class Riding extends StatelessWidget {
  const Riding({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("라이딩 화면"), centerTitle: true,),

    );
  }
}
